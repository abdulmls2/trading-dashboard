Done:

Trade History Table:
-sort the trades by earliest date   --------------------------------
-fix "Showing 1 to 1 of 1 results" -----------------------------------
-only view your trades not all users  ----------------------------

Performance:
-select year (seperate button)  ---------------------------

Profile:
-Welcome username text  --------------
-Create profile page and be able to change your profile data  -------------


Admin Dashboard:
-view journal in full screen with all features just like normal user views, and let admin edit journal colors and write -----------
-"last journaled", and be able to sort all users by it  --------------------
-define which confluences/time/pair/... allowed to which user (custom rules for each user ) ----------------
-if rule not respected flag it and alert admin  ------------------
-view all violations section  --------------
-also show to user trades which he violated with a flag  -------------

-import/export trades from excel  ------------------------------
-export data , in excel spreadsheet format  ------------------------------
-Trade Analysis / Connect with PIP  ------------------------------ 
-Trade Data Analysis (good and bad trades, see which group of confluences work best, how many times it was used ext) -------
-Calendar to see your traded days  -------------------------------
-add sl tp and rr auto ( 15,75,5) -------------------------------
-Trade link clickable -------------------------------
-trade analysis layout -------------------------------
-comment field become large vertically , make it large horizontally  -------------------------------
-Forgot Password field  ----------------------------------
-Change currency   ---------------------------------------
-Summary of trades analysis ------------------------------
-Compounding page  ---------------------------------------

----------------------------------------------------------------------------------------------------------------------------------
To fix:
-make the data of trade analysis more accurate when sending to prompts (send sentences to data maybe ext)
-group of confluences analysisc chart also show negative win rate
-fix the buttons size , make it same (history table)
-fix github, terminal errors (audit fix, vulnerabilties ext, packages update)
-Custom notes
-better trade selection, be able to select month and multiple trades (maybe within the form a seperate page)
-fix the manual import trades ( empty fields and coma for numbers ( $1,254) ) 
-add refresh button on both journals

New features:


AFTER:


---------------------------------------------------------------------------------------------------------------------
-search how to optimise database, queries, columns (because we wil have thousands of columns saved to the database) indexes ,pagination ,caching, ...

Redesign:
Journal:
-only fetching 30 trades /pagination  
-caching current months trade

Performance Overview:
-only fetching data of the selected month

Calendar:
-Only fetching data of the selected month

Trades Analysis:
-Only fetching data of the selected month

TODO REDESIGN:
-Improve speed of server side filtering
-Because we catch current months trade from journal, we can use this cache data for all other pages for current month
-Avoid sending requests / refresh each time we leave the tab or go different page within journal
-Create rpc function or table summary for Performance Overview, because when user selects all months and years we will be sending a lot of data this is not good
-For Global Analysis do optimisations
-For any admin dashboard related page do optimisations (reports, ext )